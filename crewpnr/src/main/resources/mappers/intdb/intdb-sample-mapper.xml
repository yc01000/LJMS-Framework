<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lj.crewpnr.mapper.intdb.INTDBSampleMapper">

	<select id="select" parameterType="string" resultType="com.lj.crewpnr.vo.sample.ARMSLogCrew">
		/* com.lj.crewpnr.mapper.intdb.INTDBSampleMapper.select */
		SELECT * FROM ARMS_LOG_CREW WHERE STAFF_NUM = #{staffNum} AND ROWNUM = 1
	</select>
</mapper>
